# 复制为 .env 并修改（chmod 600 .env）
PE_AUTH_MODE=session
PE_AUTH_PASSWORD=请改成你的强密码
PE_AUTH_SECRET=请改成随机串（生成：openssl rand -hex 32）
PE_AUTH_TTL=43200
PE_AUTH_MAX_ATTEMPTS=5
PE_AUTH_LOCKOUT=15
PE_FORCE_SECURE=1
